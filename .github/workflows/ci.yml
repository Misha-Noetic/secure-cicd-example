name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs when a new push arrives on the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: read
  security-events: write  # Required for Clippy SARIF upload to Security tab

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry & build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install SARIF tools
        run: cargo install clippy-sarif sarif-fmt

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run Clippy (linter + SARIF)
        run: |
          cargo clippy --all-targets --all-features --message-format=json \
            | clippy-sarif | tee rust-clippy-results.sarif | sarif-fmt
          # Re-run to get exit code (piped JSON above swallows it)
          cargo clippy --all-targets --all-features -- -D warnings

      - name: Upload Clippy SARIF to Security tab
        if: always() && hashFiles('rust-clippy-results.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: rust-clippy-results.sarif
          wait-for-processing: true

      - name: Build
        run: cargo build --release

      - name: Run tests
        run: cargo test --verbose

  # Rust-specific security advisory check (RustSec database)
  cargo-audit:
    name: Cargo Audit (RustSec)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        run: cargo install cargo-audit

      - name: Generate Cargo.lock
        run: cargo generate-lockfile

      - name: Run cargo-audit
        run: cargo audit
